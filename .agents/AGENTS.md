- **STRICT VERSION CONTROL RULE**: NEVER, under any circumstances, execute `git commit` or `git push` unless the user explicitly tells you to do so in their most recent request. Attempting to fix a past commit or auto-pushing work is strictly prohibited without first asking for and receiving a clear "okay". **CRITICAL ADDITION**: Do NOT assume that user approval to write or modify code is also approval to commit it. Approval to write code is NOT approval to commit code. You must stop and wait for a dedicated "commit and push" command.
